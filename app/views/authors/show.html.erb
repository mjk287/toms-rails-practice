<h1><%= @author.name + " (b." + @author.birth_year.to_s + ')' %> </h1>
Books:
<ul>
<% @author.books.each do |book|  %>
  <li><%= link_to(book.name, book_path(book)) %></li>
<% end %>
</ul>
<%= link_to("Edit " + @author.name, edit_author_path(@author)) %>
<%= form_tag author_path(@author.id), method: "delete" do %>
<%= submit_tag "Delete #{@author.name}" %>
<% end  %>
